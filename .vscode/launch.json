{
    "configurations": [
        {
            "name": "Attach to Chrome",
            "port": 29097,
            "request": "attach",
            "type": "pwa-chrome",
            "webRoot": "${workspaceFolder}"
        }
    ],
    "version": "0.2.0"
}